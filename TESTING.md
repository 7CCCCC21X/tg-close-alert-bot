# 测试与交付状态

版本：1.12.1

## 已完成

- 1.12.1：上证收盘后的 A50 锚点优先取带日期的 1 分钟 K；缺历史分钟时退到同合约、同日 15:00 的 5 分钟 K，并把近似标签、来源与价格持久化。旧版无来源锚点先重新核验；不同来源的 A50 连续合约与 CFD 不混算；没有历史锚点时不拿“昨结”冒充收盘时价。上证收盘后至日 K 最终确认前显示待确认。校正 A50 夜盘至 16:45–次日 05:15，休市时也核对最后一口价是否属于最近完整时段。离线回归覆盖错过 9/24 锚点后 9/26 找回 5 分钟线、重启恢复、旧版记录迁移、来源不一致和旧价暂停概率、精确锚点升级。
- 1.1.0：启动时通过 `setMyCommands`/`setChatMenuButton` 注册命令菜单；命令处理重构为分发表，离线模拟测试覆盖全部命令、菜单载荷校验与提醒循环。`/threshold` 不带参数发送 inline keyboard 档位卡片，按钮回调（callback_query）只接受管理员，点选后即时生效并原地更新卡片。`/setclose` 支持多行/逗号分隔批量设置，整批校验后一次写入。每条可带收盘时间，手动/日 K 基准的收盘时间显示在 /status 与提醒中。`/setexchange` 记录证券交易所收盘价，可带货币代码，非美元货币只展示不计算涨跌。`EXCHANGE_TICKERS` 自动从东方财富（A 股/港股）和 Naver（韩交所）日 K 接口获取标的股票最近已完结交易日收盘价，离线测试使用样例响应覆盖解析、完结交易日判定、缓存、失败保留旧值和手动覆盖。
- 1.2.0：去掉官方收盘价；交易所收盘价按 Frankfurter/open.er-api 汇率折成美元后显示“相对交易所”涨跌（HK0625USDT 为 quanto，直接比较）；/status 与提醒改为 HTML 加粗 + 🔴🟢⚪ 涨跌标记（COLOR_STYLE），所有动态文本经 HTML 转义。离线测试覆盖汇率获取/回退/缓存、折算、转义与排版。
- 1.2.1：交易所收盘价来源多级回退（东方财富→腾讯→新浪，各重试一次，请求错开 0.5 秒），最近成功值持久化到 SQLite；离线测试覆盖腾讯/新浪报文解析、回退顺序、重试次数、持久化恢复与全部失败时保留旧值。
- 1.2.2：最新成交价过期时改用币安标记价（premiumIndex）判断并标注，避免成交稀疏合约被误判为行情中断；离线测试覆盖新鲜/过期/无标记价/标记价也过期、接口合并与失败降级、提醒与状态文案。
- 1.2.3：核对并修正收盘时间：港交所收市价改为收市竞价结束的 16:10；韩交所显示当地 15:30 与北京 14:30 两种时间；币安日 K 标注 UTC 日与 00:00 UTC 换日；离线测试覆盖各市场收盘标签、港股竞价前不算完结、持久化恢复。
- 1.2.4：交易所收盘价附带昨收与“股票当日”涨跌，价格行显示币安指数价，便于区分合约溢价与数据误差；离线测试覆盖昨收提取（日 K/实时报价）、文案、持久化与指数价解析。
- 1.3.0：新增恒指期货主力（含夜市）报价与高低水（东方财富→新浪，现货东方财富→腾讯→新浪），显示在 /status 顶部和港股标的的提醒中；离线测试覆盖时段判定、两种报文解析、回退、缓存、失败保留旧值与文案。
- 1.4.0：新增 Hyperliquid 参考价（info 接口 metaAndAssetCtxs，按 dex 读取 HIP-3 市场），显示标记/预言机/24h/资金费率与币安相对 HL 的偏差，quanto 合约按汇率折美元；离线测试覆盖配置解析、报文解析、每 dex 一次请求、未找到市场提示、失败保留旧值、状态与提醒。
- 1.4.1：新增 KOSPI 综合指数（Naver 实时指数接口→东方财富 100.KS11），显示在 /status 顶部与韩股标的提醒中；离线测试覆盖 Naver 报文（含跌幅方向）、东方财富报文、回退、缓存、失败保留旧值与文案。
- 1.5.0：新增 /mode exchange 基准模式：基准取币安合约在标的交易所收盘时刻的 1 分钟 K 线收盘价（无成交则标记价 K 线），收盘时刻与自动获取的交易所日 K 共用时间戳，缺失时按工作日日历推算；离线测试覆盖 K 线取价与回退、缓存、收盘时刻推算（周末/跨日/韩国时区）、完整提醒周期、/mode 命令与状态文案。
- 1.5.1：配置了 EXCHANGE_TICKERS 时默认基准改为 exchange_close，使“相对基准”与“相对交易所”同一时点；/setclose 在非手动模式下的提示改为显示当前模式名。
- 1.6.0：状态与提醒改为树形紧凑版式（每行“来源 价格（时间·数据源）→ 偏差”），价格位数收敛，恒指/KOSPI/HL 行精简，汇率移到页脚，错误提示截短；日 K 模式下提示 /mode exchange。离线测试全部改为新版式断言。
- 1.6.1：恒指期货行改为“→ 恒指（收盘）偏差（高/低水）｜恒指当日｜期货昨结”，现货报价同时解析昨收；KOSPI 行改为“→ 昨收 涨跌”。
- 1.6.2：恒指/KOSPI 行的标题与点位加粗，时间带日期，数据日期早于当天时标“非今日数据”。
- 1.7.0：新增 KOSPI 200（Naver KPI200）并与 Hyperliquid xyz:KR200 永续对比（HL_INDEX），显示在 KOSPI 行下方和韩股标的的提醒中；离线测试覆盖配置、双指数抓取、KPI200 单独失败、文案与状态/提醒位置。
- 1.7.1：恒指期货首选 etnet 页面（港交所合约，日市/夜市取最新一段，采用其公布的高低水），新浪 hf_HSI 识别为 CFD 并降为兜底且标注；离线测试用 etnet 样例页覆盖日市/夜市选择、平水、Big5、缺失报错与文案。
- 1.8.0：新增收盘涨跌概率（状态中的 🎲 行与 /prob 明细）：代理收益映射有效价、按剩余交易分钟缩放的 σ、正态概率与平盘各半；离线测试复现 KOSPI 28.2¢/71.8¢、恒指 49.5¢/50.5¢ 与 σ 敏感性（14.77%/34.47%），并覆盖最小跳动、交易时段、波动率估计与合成、锚点获取与节流、恒指日市/夜市/盘中三种情形、/prob 与状态文案。
- 1.8.1：涨跌提醒附带该合约的收盘概率 🎲 行；离线测试覆盖提醒中的位置。
- 1.9.0：新增上证指数（腾讯→新浪→东方财富）与 A50 期货（东方财富 104.CN00Y→新浪 CFD）代理、15:00 分钟线锚点（持久化）、β 映射与上证 σ；新增交易所假期表（目标日跳过、假日按半天方差计入）。离线测试覆盖配置、三种上证报文与两种 A50 报文、时段、锚点获取与重启恢复、国庆跨假期的目标日与 σ、盘中/盘后/假期三种情形、状态与 /prob。
- 1.10.0：新增只读概率网页（标准库 asyncio HTTP，/p/<令牌> 与 data.json，/health）和 /web 命令；离线测试用真实 TCP 请求覆盖路由、令牌、HEAD/POST/畸形请求、500 不中断服务、令牌持久化与 /web 文案，并用 Chromium 无头浏览器在手机宽度渲染页面核对卡片内容。
- 1.10.1：网页每张卡片显示目标收盘时刻与逐秒倒计时（按服务器时间校正）；离线测试核对各市场收盘时刻与标签，并在浏览器中确认倒计时数值与跳动。
- 1.11.0：网页改版（指数/合约分组、紧凑卡片、可折叠计算明细且刷新后保持展开）；上证在缺少 15:00 锚点时按 A50 夜盘所属日判断，使用当天日盘结算价或收盘价兜底并注明；恒指 σ 增加腾讯日 K 源，指数 σ 每个窗口只尝试一次。离线测试覆盖三种兜底、分组字段、明细展开跨刷新保持。
- 1.12.0：交易所收盘时刻基准只由带日期的交易所日 K 确认，不再按工作日日历推算；基准保持到下一次已确认收盘替换（30 天安全上限，存 SQLite），长假/周末/数据源故障期间沿用并在逾期时标“收盘待确认”；不带日期的报价不会覆盖已带日期的收盘。上证盘后参考收盘改用带日期的 sh000001 日 K（腾讯→东方财富，校验代码），未确认前显示“收盘价待确认”且不输出概率；实时行情校验代码（sh000001/000001），盘中超过 10 分钟未更新（午休不计）或 A50 在交易时段超过 10 分钟未更新时不输出新概率；A50 时段识别周末休市。参考数据改为各自独立的后台任务（独立线程池、单次 300 秒超时），5 秒提醒循环只读最近结果。`.env.example` 等模板补上 `HK0625USDT=hk:00625:same`。新增 `tests/`（21 组离线测试 + `run_all.py`，`offline.py` 禁止真实 HTTP），Docker 构建运行全部测试。离线测试覆盖 9/24 收 3888.37、9/25–27 休市、9/28 开盘前仍用 09-24 收盘的完整场景，以及日 K 未出时的“待确认”、代码校验、A50 过期、挂起/报错的参考源不拖慢提醒。
- 1.12.1：修正恒指期货取段：etnet 正文里没有日市/夜市时间（在图表图片里），以前两段同时被记为“当前时间”，周末取到了日市 24,522 却标成夜市，概率又拿日市的前收市 24,691（前一日收盘）当锚点，把当日跌幅重复计入（有效价 24,342，涨 23.5¢）。现按“夜市前收市 = 日市最新价”判断夜市在后，时间取该段收市时刻；港期时段识别周末与 HOLIDAYS_HK，非交易时段标“（已收市）”；恒指收盘日跳过港股假日。离线测试按 09-26 截图数据复现：取夜市 24,501、锚点 24,522、有效价约 24,489（−0.086%）。
- `tests/` 下 21 组离线测试（`python tests/run_all.py`）全部通过；Python 3.11 与 3.12 均已运行，3.11 下另含 Chromium 无头浏览器渲染检查。
- 覆盖严格 ±1% 边界、小数计算、涨跌双向提醒、去重、周期提醒、扩大档位、反向触发和重新武装。
- 覆盖 UTC 换日、完整日 K 选择、拒绝使用更旧日 K、日 K 缺失、新上市空数据、手动基准过期。
- 覆盖最新成交时间戳过期/异常、合约缺失、单项故障不阻断其他合约、网络恢复。
- 覆盖订阅和群组话题隔离、管理员授权、禁止隐式订阅、暂停/取消、SQLite 重启持久化。
- 覆盖 Telegram 失败后不标记为已成功发送，以及 Token 日志脱敏。
- 提供 Dockerfile；Docker 构建步骤运行 `tests/run_all.py`，任一组失败即构建失败（镜像里没有 Chromium，浏览器检查自动跳过）。
- 提供 Railway 配置文件、环境变量模板和中文部署说明。

## 尚未完成的真实环境验证

- 本开发环境的出站代理拦截了东方财富、Naver、雅虎等行情站点，`EXCHANGE_TICKERS` 的四个接口、汇率接口、恒指期货接口与 Hyperliquid 接口未在本环境做真实联通测试（交易所接口已在用户 Railway 部署中验证可用）；部署后请运行 `python main.py --check` 或看 `/status` 确认。

- 生成环境无法解析并访问 Binance API 主机，因此未完成这四个合约的实时 API 联通测试。已参考官方 API 文档编写，程序启动后会逐个验证是否返回所需合约与有效数据。
- 未取得 Telegram Bot Token，因此没有真实发送 TG 消息。推送路径使用模拟测试覆盖。
- 未取得已连接的 GitHub/Railway 账户权限，因此没有替用户创建仓库、提交代码、创建云服务或实际部署上线。
- 当前环境未执行 Docker 镜像构建；Railway 构建时会执行。
- 本项目不自动获取股票交易所的正式昨收价；不能把“自动币安日 K 模式”宣传为“自动股票官方昨收模式”。

## 上线验收

1. 确认选择的参考价口径确实是你要监控的口径。
2. Railway 构建测试通过，环境变量正确，单副本运行，挂载 `/data` Volume。
3. 机器人 `/id` 能回复，设置管理员后 `/subscribe` 能成功。
4. `/status` 显示四个准确合约代码、当前成交价、正确基准日期和值、较新的行情时间。
5. 发送 `/test` 确认消息到达正确私聊或群组话题。
6. 至少核对一次真实越界提醒；再重新部署一次，确认订阅还在、没有异常重复提醒。

测试使用虚构价格，不代表任何实际股票或合约报价。成功的离线测试不等同于真实行情、Telegram 网络和云部署已端到端验证。
